# S1 Experiment Record Through 2026-07-20

Setup: a frozen π0.5 flow VLA with DSRL and SAC operating in a 32-dimensional flow-noise space on LIBERO. This fork's `libero_10` contains an expanded set of 2519 task variants. Full closed-loop configurations use sparse success rewards, 500k training steps per seed, 3 seeds, and an H100 80 GB GPU. The report marks 150k-step feasibility runs and seed-0-only results explicitly.

## 1. Question and claim

**Question:** Can the field's open-loop evidence standards—capacity, separability, and probe accuracy in the style of PIVOT/MOKA/SoM/R3M—predict closed-loop RL utility when interpretable channels such as visual arrow overlays, keypoints, and text become control surfaces for a frozen VLA?

**Motivation:** Three established observations define the problem. (i) DSRL shows that a frozen flow VLA can improve under RL using noise alone: SAC learns a policy in the 32-dimensional initial-noise space without changing model weights. (ii) Human-readable ports beyond noise, including arrows drawn on the image and edits to instruction text, already control large models in PIVOT/MOKA/SoM and are justified through open-loop decodability. (iii) NLP and LLM work on probing, ITI, and RepE distinguishes information that can be read from information that can be written back. Connecting the ports in (ii) to the RL framework in (i) exposes the untested extrapolation from open-loop evidence to RL usability.

**Current claim:** A read–write gap separates open-loop decodability from closed-loop steerability. Open-loop metrics have no predictive value for RL utility and can be anti-predictive. Control authority—the action displacement relative to the task's behavioral gap—is necessary. Its magnitude is not sufficient: the **structure of writable directions** must spread authority across directions SAC can exploit. Concentrating high authority in a few hypersensitive directions is actively harmful.

## 2. Metrics and arms

| Concept | Operational definition |
|---|---|
| read, or open-loop decodability | capacity, a Gaussian MI proxy; directional separability, sep = −cos, which is scale-invariant and therefore blind to magnitude |
| write, or closed-loop steerability | authority is measured by the geometry probe's jump: the maximum ‖Δaction‖ between adjacent control-grid cells, obtained through inference alone in minutes; the cross-task ratio is jump / same-task noise_2d jump |
| methodological gate | valid Step 0 baseline: untrained SAC with fresh noise at every replan, baseline success ≤ 0.3, and no free-retry ceiling; rule 2: a same-task noise_2d collapse creates a “2-dimensional control surface is insufficient” confound and invalidates the point; every key prediction is preregistered before execution |

### Arms and injection mechanisms (`train_utils_sim.py:569-605`)

- **noise:** RL outputs 32 dimensions and fully replaces the initial flow noise. This is the original DSRL arm and upper-bound reference.
- **noise_2d:** RL outputs 2 dimensions. Each replan still samples fresh 32-dimensional random noise and adds the offset `(fixed random orthogonal 32 × 2 basis @ z) · √32`. RL can move the full noise cloud only along a fixed 2-dimensional plane, while the other 30 directions remain random. Degrees of freedom and background randomness match overlay and text; only the port differs, giving a dimension-matched control.
- **overlay:** Interpret 2 dimensions as direction `(dx, dy)`, draw an arrow on the camera image, and feed the result to the model as a PIVOT-style visual prompt.
- **text:** Discretize 2 dimensions into direction words such as “move left” and append them to the instruction. π0.5 ignores the input, making this dead-channel negative control the chance floor.
- **noise_2d_lowauth:** Use the noise_2d injection with a manually selected basis of low- or high-sensitivity directions, making authority an intervention variable.

### Training-free measurement procedure

Fix a scene, set the control level, and run inference approximately 24 times per level to record a level-to-action-samples table. The three metrics answer distinct questions. Capacity and sep are implemented in `sanity_channel_capacity.py`; jump is implemented in `control_surface_geometry.py`.

- **capacity:** The Gaussian MI proxy asks whether the control level can be inferred from the action, in bits. It scores any stable distributional difference, independent of displacement magnitude.
- **sep = −cos(Δleft, Δright):** Tests whether mean action displacements for opposing intents point in opposite directions. Cosine normalization removes magnitude.
- **jump, from the geometry probe:** On a 5 × 5 control grid, measure the maximum mean absolute action displacement ‖Δa‖ between adjacent cells. This is the only magnitude-sensitive metric. The probe also reports SNR above the noise floor and R²_lin for surface smoothness.

## 3. Experiment timeline

| # | Experiment | Date | Scale | Result |
|---|---|---|---|---|
| 1 | Gate 1 open-loop capacity | 06-20 | 8 tasks × 5 channels × n = 24 | `overlay_arrow` reached C = 1.88 bits, far above the dead text/state port at 0.00 bits and close to the noise 2D slice at 1.95 bits. |
| 2 | Directional controllability scan A | 06-20 | 10 scenes × 3 variants | 50/120 combinations were reliably controllable; valid regime = keypoint @ 0.5 / arrow @ 1.0; anchor 838 arrow sep = +1.00 |
| 3 | Gate 2 first pass (t237) | 06-22 | 12 per-seed runs | Invalid task: all four arms were at ceiling, including dead-channel text at AUC = 0.95; fresh-noise free retries and baseline success of 0.62 left no headroom. |
| 4 | Gate 2 second pass (t838) | 06-23 | 12 per-seed runs | **Primary hypothesis falsified; supported reversal:** overlay at AUC = 0.206 and SR = 0.30 was the worst arm, below text at AUC = 0.342, while noise_2d reached 0.626/0.93. The Gate 1 capacity ranking reversed in closed loop. |
| 5 | Mechanism probe, geometry | 06-23 | 3 surfaces × 5 × 5 grid | Cause: authority, not non-smoothness. All three surfaces had R² ≈ 0.5; overlay jump = 0.18 versus noise_2d jump = 1.98, an 11× difference; text SNR ≈ 0. A strength sweep from 0.5 to 2 raised jump only linearly while R² fell, so authority could not be added cleanly. |
| 6 | sep ⊥ authority breadth | 06-24 | 8 tasks × 2 channels | Across 16 rows, Pearson r = +0.25 and Spearman ρ = +0.07 showed no rank prediction; equal sep corresponded to a 60× authority range, and negative sep could retain authority at t1958. |
| 7 | B 5-task closed-loop monotonicity | 06-30 | 36 per-seed runs | ratio → overlay AUC: 1.008 → 0.998 / 0.519 → 0.959 / 0.385 → 0.986 / 0.088 → 0.206 / 0.046 → 0.205, with breakpoints (0.09, 0.39). The successful side was invalidated by the 07-10 Step 0 audit. |
| 8 | lowauth 2 × 2 (t838) | 07-02 | 3 per-seed runs | Native control with low authority collapsed to 0.333/0.387, approximately the level of overlay and text, supporting **authority rather than nativeness as the driver**. |
| 9 | retrospective Step 0 audit | 07-10 | — | **Major invalidation:** B's 3 successful tasks, 276, 15, and 1170, had saturated Step 0 results, with overlay at 1.0 for all 3 seeds. The free-retry ceiling invalidated the 5-task monotonic result and left 0 clean points on the successful side. |
| 10 | First preregistered scan | 07-03→08 | 4 tasks, 27 per-seed runs | Inconclusive, 0/4: 673 failed even with noise; 612 and 786 hit rule 2; 1604 passed through a Step 0 loophole and was the only scored point, with a wrong prediction. |
| 11 | effbase prescreen chain | 07-11 | 11 candidates | 5 passed the Step 0 gate; 3 tasks, 555, 876, and 938, passed the 150k-step feasibility test, closing the first scan's loophole. |
| 12 | Second preregistered scan, Phase C | 07-11→15 | 21 per-seed runs | Inconclusive, 0/3: noise_2d collapsed at 500k on all three tasks, with respective results of 0.067, 0.275, and 0.102, so rule 2 invalidated every point. The 150k-step feasibility signal did not hold at 500k. Across both scans, 0 of 8 sampled tasks yielded a valid point. |
| 13 | Authority dose response | 07-15→20 | 30 per-seed runs | Both locked predictions were falsified; see §4.5. |

Closed-loop execution totaled approximately 141 per-seed runs at 500k steps, plus 150k-step short runs and open-loop probes.

## 4. Core results

### 4.1 Reversal: the best open-loop channel was the worst closed-loop surface (four arms on t838)

| Arm | AUC / final SR | Open-loop metric |
|---|---|---|
| noise 32D | 0.797 / 0.97 | — |
| noise_2d | 0.626 / 0.93 | sep is undefined for random directions |
| text, dead channel | 0.342 / 0.33 | C = 0.00 bits |
| **overlay** | **0.206 / 0.30** | **C = 1.88 bits, sep = +1.00, highest overall** |

The channel with the highest capacity and perfect directional separability was the worst control surface, below the dead channel. A healthy dimension-matched noise_2d arm excludes task difficulty and dimensionality as explanations.

### 4.2 Mechanism: authority, not surface roughness

Geometry probe values, reported as SNR / R²_lin / jump, were 6.4 / 0.52 / **0.18** for overlay, 19 / 0.49 / **1.98** for noise_2d, and 0.11 / 0.58 / 0.04 for text. R² ≈ 0.5 for all three surfaces falsifies the “overlay is too rough to optimize” account. Overlay is detectable, with SNR > 1, but cannot move the policy; its jump is 11× lower. In the strength sweep from 0.5 to 2, jump increased only from 0.12 to 0.29. Extrapolation requires strength ≈ 13, where the arrow fills the image, while R²_lin deteriorates above 1 because of occlusion distortion. Scaling the overlay cannot supply causal authority within the usable strength range.

### 4.3 Decoupling: sep ⊥ authority across 8 tasks and 16 rows

Pearson r = +0.25 was driven by one point; Spearman ρ = +0.07 showed no rank relationship. Equal sep corresponded to a 60× authority range: t276 arrow, sep = +0.98 and ratio = 1.0, versus t758 keypoint, sep = +0.85 and ratio = 0.016. Negative sep retained authority at t1958, with sep = −0.04 and ratio = 0.54, and at t1170, with sep = −0.24 and closed-loop AUC = 0.986. Open-loop separability cannot rank candidate control surfaces.

### 4.4 Attribution: authority rather than nativeness in the t838 within-task 2 × 2

| | High authority | Low authority |
|---|---|---|
| Native | noise_2d 0.627/**0.940** | lowauth 0.333/0.387 |
| External | overlay at task 276: 0.998, later invalidated by the Step 0 audit | overlay: 0.206/0.253 |

Holding nativeness fixed and removing authority collapses performance to the overlay/text floor, identifying authority as the driver. Within the native 32-dimensional space, authority varies by two orders of magnitude: 1.985 versus 0.013.

### 4.5 Dose response: direction structure dominates authority magnitude (completed 07-20; both preregistered predictions falsified)

The basis-construction probe found an effective rank of approximately 2 in the J^T J spectrum on all 4 tasks: the top 2 eigenvalues were O(1), while the rest were O(10⁻⁵). A random 2-dimensional basis therefore already reached near-maximal authority at jump ≈ 2.0, while the mid- and low-authority settings were approximately equal.

Mean success rate over the final 50% of training, averaged across 3 seeds unless marked otherwise:

| Task | noise 32D | native-low (jump ≈ 0.01) | mid | **native-high (jump ≈ 2.0)** | **random-2D (jump ≈ 2.0)** |
|---|---|---|---|---|---|
| 838 | ✓ | 0.375 | 0.388 | **0.000** | **0.94** |
| 876 | 0.68 | 0.065 | 0.055 | 0.000 | 0.10 |
| 555 | 0.951 | 0.206 | 0.251 | 0.260 (seed 0 only) | 0.275 (seed 0 only) |

- Prediction ①, “engineered native-high reproduces random,” was **falsified in the opposite direction**. On 838, at the same jump of 2.0, random directions learned to 0.94 while the top 2 eigenvectors produced 0.00 across all 3 seeds. Equal authority produced opposite outcomes. **Direction structure** is the deciding variable: displacement concentrated in 2 hypersensitive directions creates bang-bang control that cannot make fine adjustments, while displacement spread across many directions remains controllable.
- Prediction ②, “within-task jump is monotonic,” was **falsified**. On 838, the near-zero-authority settings at 0.375 and 0.388 exceeded the high-authority result of 0.000. The near-zero result is a free-retry floor rather than learning; strong perturbations along high-sensitivity directions actively move the policy away from the task and fall below the floor.
- Noise passed the validity gate on all three tasks, producing clean comparisons within the 2-dimensional control space. The same-task, equal-jump high-versus-random contrast on 838, 0.00 versus 0.94, directly supports the direction-structure claim.

## 5. Falsification and invalidation history

1. t237 invalid task, 06-22: the negative control did not die, exposing the fresh-noise free-retry confound.
2. t838 primary hypothesis falsified, 06-23: the overlay-capacity account failed and the supported reversal redirected the analysis to authority.
3. Step 0 audit, 07-10: the successful side of the 5-task monotonic result was invalidated as a ceiling artifact, establishing the valid Step 0 baseline gate.
4. Two preregistered scans, 07-08 / 07-15: both were inconclusive. The cross-task ratio prediction was structurally untestable because none of 8 draws from a 130-task low-base pool simultaneously provided headroom, 2-dimensional controllability, and no ceiling.
5. Dose response, 07-20: both predictions were falsified, replacing a monotonic-authority law with the stronger direction-structure result.

Every prediction was locked before execution in `s1_prediction_preregistration.md` and scored literally.

## 6. Claim status

**Supported:**

1. Open-loop decodability standards fail for frozen-policy RL control-surface selection, and their rankings can reverse, as shown by the four-arm 838 result and the 8-task sep ⊥ authority decoupling.
2. Authority is necessary: the 838 2 × 2 collapses when authority is removed, independent of nativeness.
3. Authority is insufficient, and concentrated high authority is actively harmful. At equal jump, distributed random directions outperform concentrated top eigenvectors by 0.94 versus 0.00 on 838.
4. The noise-to-action sensitivity spectrum of frozen π0.5 has an effective rank of approximately 2 on all four measured tasks.
5. Methodological findings: enforce the valid Step 0 baseline gate; run the noise_2d control to 500k across multiple seeds; do not extrapolate a 150k feasibility signal.

**Falsified or invalidated:** Cross-task monotonic authority on the successful side; within-task jump monotonicity; equivalence between engineered top eigenvectors and random directions; closed-loop meaning in the Gate-1 capacity ranking; and the “rough overlay surface” account.

**Boundary:** One policy, π0.5, and one simulator, LIBERO. The clean positive case is concentrated on 838. Task 555 has large seed-dominated variance. Evaluation uses 10 episodes at 0.1 resolution. The “distributed directions enable control” mechanism combines the within-task contrast on 838 with consistent collapse across three tasks; it is not a cross-task scan.

## 7. Paper framing (`s1_paper_framing.md`)

- Title: **Decodable Is Not Steerable:** Open-loop capacity and separability do not predict the utility of an RL control surface for a frozen VLA.
- Introduction: connect the NLP probing critique and the read–write gap in LLM steering to the first quantitative embodied closed-loop instance.
- Evidence-bounded claims: (i) open-loop metrics fail and can be anti-predictive; (ii) authority is necessary through the 2 × 2, while direction structure explains sufficiency through the same-jump contrast; (iii) the geometry probe's jump is an inference-only estimator.
- Related work: evaluate the extrapolation from PIVOT/MOKA/SoM/R3M evidence standards to RL control-surface selection, not those original systems; place authority against the Gramian and empowerment-capacity line without claiming its invention; connect the read–write gap to ITI/RepE/V-info.

## 8. Data and infrastructure

- Data: `outputs/{channel_capacity_gate1,scene_controllability,capacity_vs_authority,control_surface_geometry_sweep,gate2_matrix,gate2_auth_t*,gate2_lowauth_t838,gate2_pred_t*,feas_t*,dose_basis,dose_t*}/`; aggregate with `analyze_gate2.py`.
- Reports: `s1_interface_capacity_gate{1,2}_report.md` and `s1_prediction_preregistration.md`, containing all registrations and scores.
- Batch infrastructure: idempotent RESUME orchestration in `run_gate2_matrix.sh`. The workflow recovered from all 7 platform container reclamations without data loss through persistent storage and the automatic `xyh_watchdog.sh`. The watchdog detects failure from both Mac and ssh2, rebuilds the container headlessly, and restarts the chain.
